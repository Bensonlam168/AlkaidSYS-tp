# Zed 编辑器中 Auggie 使用问题分析报告

**报告日期**: 2025-11-24  
**问题类型**: 对话历史丢失、快捷键失效  
**影响范围**: Zed 编辑器 + Augment AI (Auggie)  
**操作系统**: macOS

---

## 📋 问题概述

用户在 Zed 编辑器中使用 Auggie (Augment AI 助手) 时遇到两个主要问题:

1. **对话历史丢失**: 关闭 Zed 窗口或编辑器后，无法找回之前的 Auggie 对话记录
2. **快捷键失效**: 无法使用 `Cmd+P` 进行提示词增强功能

---

## 🔍 深度分析

### 1. 对话历史持久化问题

#### 问题根源

根据 Augment 官方文档和 CLI 参考，Auggie 的对话历史保存机制如下：

**CLI 模式下的会话管理**:
- Auggie CLI 支持 `--continue (-c)` 标志来恢复之前的对话
- 使用 `--dont-save-session` 可以禁止保存会话到本地历史
- 使用 `--delete-saved-sessions` 可以删除所有已保存的会话

**IDE 集成模式的限制**:
根据搜索结果和 Reddit 讨论，在 IDE 中使用 Auggie 时存在以下情况：
- Auggie 的对话历史在某些情况下不会自动持久化
- 用户报告需要手动"开始新对话并删除所有之前的聊天历史"来重置状态
- 这表明 IDE 集成版本的会话管理与 CLI 版本不同

**Zed 编辑器的特殊性**:
- Zed 是一个相对较新的编辑器，其 AI 集成可能还在完善中
- Zed 官方文档提到了 Agent Panel 和 Text Threads 功能，但这些主要针对 Zed 自带的 AI 功能
- Augment 在 Zed 中的集成可能是通过扩展或插件实现的，而非原生支持

#### 技术原因

1. **会话存储位置不明确**: 
   - Auggie CLI 会话存储在本地文件系统
   - IDE 集成版本可能使用不同的存储机制
   - Zed 可能没有提供持久化 AI 对话的标准接口

2. **窗口生命周期管理**:
   - 关闭 Zed 窗口可能会清除内存中的对话状态
   - 没有自动保存机制将对话写入磁盘

3. **扩展 API 限制**:
   - Zed 的扩展 API 可能不支持持久化存储
   - Augment 扩展可能无法访问必要的存储接口

### 2. Cmd+P 快捷键失效问题

#### 问题根源

**快捷键冲突**:
- `Cmd+P` 在大多数编辑器中是"快速打开文件"的标准快捷键
- Zed 编辑器也使用 `Cmd+P` 作为命令面板/文件搜索快捷键
- Auggie 的"提示词增强"功能可能需要不同的快捷键绑定

**Zed 快捷键系统**:
根据 Zed 官方文档 (https://zed.dev/docs/key-bindings):
- Zed 支持自定义键位绑定
- 键位绑定配置文件位于用户配置目录
- 可以为不同的上下文设置不同的快捷键

**Augment 的提示词增强功能**:
根据文档，Augment 提供以下交互方式：
- `@` 提及 (Context menu) - 用于添加上下文
- User Guidelines - 存储在 IDE 本地的用户偏好
- Rules - 存储在仓库中的项目规则

但文档中没有明确提到 `Cmd+P` 作为提示词增强的快捷键。

---

## 💡 解决方案

### 方案 A: 使用 Auggie CLI (推荐)

**优势**:
- 完整的会话管理功能
- 支持 `--continue` 恢复之前的对话
- 明确的会话存储机制
- 不依赖编辑器的集成

**实施步骤**:

1. **安装 Auggie CLI** (如果尚未安装):
   ```bash
   # 通过 Augment 官方渠道安装
   # 具体安装方法请参考: https://docs.augmentcode.com/cli/setup
   ```

2. **在项目目录中使用 Auggie**:
   ```bash
   cd /Users/Benson/Code/AlkaidSYS-tp
   auggie  # 启动交互模式
   ```

3. **恢复之前的对话**:
   ```bash
   auggie --continue  # 或使用 -c
   ```

4. **配置项目规则** (可选):
   ```bash
   # 创建项目规则文件
   mkdir -p .augment/rules
   echo "# 项目特定规则" > .augment/rules/project.md
   ```

5. **设置用户指南** (可选):
   ```bash
   # 用户指南存储在 ~/.augment/settings.json
   auggie  # 在交互模式中通过 @ 菜单访问 User Guidelines
   ```

**会话管理**:
- 每次对话会自动保存
- 使用 `auggie -c` 继续上次对话
- 使用 `auggie --delete-saved-sessions` 清理历史会话

### 方案 B: 配置 Zed 编辑器集成

**适用场景**: 希望在 Zed 编辑器内直接使用 Auggie

**实施步骤**:

1. **检查 Augment 扩展版本**:
   - 确保安装了最新版本的 Augment 扩展
   - 检查扩展设置中是否有会话持久化选项

2. **配置 Zed 键位绑定**:

   创建或编辑 Zed 的键位配置文件:
   ```bash
   # macOS 配置文件位置
   ~/.config/zed/keymap.json
   ```

   添加自定义键位绑定 (示例):
   ```json
   [
     {
       "context": "Editor",
       "bindings": {
         "cmd-shift-p": "augment::enhance_prompt"
       }
     }
   ]
   ```

   **注意**: 具体的命令名称需要参考 Augment 扩展的文档。

3. **使用 Augment Memories 作为替代**:

   根据文档，Augment 提供 Memories 功能：
   - Memories 会自动记录 Agent 的重要信息
   - 可以将 Memories 导入为 Rules 或 User Guidelines
   - 这可以部分替代对话历史功能

   **操作步骤**:
   - 在提示框中点击 "Augment Memories"
   - 选择要保留的信息
   - 点击 "User Guidelines" 保存为用户指南
   - 或保存为 Rule 文件供团队共享

4. **使用 Rules 和 Guidelines 系统**:

   **User Guidelines** (个人偏好):
   - 存储位置: IDE 本地存储
   - 作用范围: 所有项目
   - 字符限制: 24,576 字符
   - 访问方式: Settings > User Guidelines and Rules

   **Workspace Guidelines** (项目规则):
   - 存储位置: `.augment-guidelines` 文件 (已废弃)
   - 新方式: `.augment/rules/` 目录
   - 作用范围: 当前项目
   - 字符限制: 49,512 字符 (Guidelines + Rules 总和)

   **创建项目规则**:
   ```bash
   mkdir -p .augment/rules

   # 创建 always 规则 (总是包含)
   cat > .augment/rules/always-project-context.md << 'EOF'
   # 项目上下文

   这是 AlkaidSYS 项目，一个基于 ThinkPHP 8.0 和 Vue 的低代码 SAAS 系统。

   ## 技术栈
   - 后端: ThinkPHP 8.0, Swoole, PHP 8.1+
   - 前端: Vue 3, Vite, TypeScript
   - 数据库: MySQL/PostgreSQL

   ## 代码规范
   - 使用 DDD 架构
   - 遵循 PSR-12 编码标准
   - 前端使用 Composition API
   EOF

   # 创建 auto 规则 (自动检测)
   cat > .augment/rules/auto-lowcode-guide.md << 'EOF'
   ---
   description: 低代码系统开发指南
   ---

   # 低代码开发规范

   当开发低代码相关功能时:
   - 字段类型定义在 `infrastructure/Lowcode/Collection/Field/`
   - 集合管理使用 `CollectionManager` 服务
   - 所有操作需要考虑多租户隔离
   EOF
   ```

### 方案 C: 混合方案 (最佳实践)

结合 CLI 和 IDE 的优势：

1. **日常开发**: 在 Zed 中使用 Auggie 进行快速查询和代码辅助
2. **复杂任务**: 使用 Auggie CLI 进行需要长对话的任务
3. **知识管理**: 将重要的对话内容转化为 Rules 和 Guidelines

**工作流程**:

```bash
# 1. 启动 Zed 进行日常开发
zed /Users/Benson/Code/AlkaidSYS-tp

# 2. 对于需要保留的对话，使用 CLI
cd /Users/Benson/Code/AlkaidSYS-tp
auggie "帮我分析低代码系统的架构"

# 3. 继续之前的对话
auggie -c

# 4. 将重要的指导原则保存为规则
# (在对话中让 Auggie 生成规则文件)
auggie -p "将我们讨论的低代码最佳实践整理成规则文件" > .augment/rules/lowcode-best-practices.md
```

---

## 🔧 快捷键配置指南

### Zed 编辑器快捷键自定义

1. **打开 Zed 配置目录**:
   ```bash
   open ~/.config/zed
   ```

2. **创建或编辑 `keymap.json`**:
   ```json
   [
     {
       "context": "Editor",
       "bindings": {
         "cmd-shift-a": "assistant::toggle",
         "cmd-shift-/": "assistant::inline_assist",
         "cmd-k cmd-i": "assistant::inline_assist"
       }
     }
   ]
   ```

3. **查看可用命令**:
   - 在 Zed 中按 `Cmd+Shift+P` 打开命令面板
   - 搜索 "augment" 或 "assistant" 查看可用命令
   - 记录命令 ID 用于键位绑定

### 推荐的快捷键方案

由于 `Cmd+P` 已被文件搜索占用，建议使用以下替代方案：

- `Cmd+Shift+P`: 命令面板 (Zed 默认)
- `Cmd+Shift+A`: 切换 AI 助手面板
- `Cmd+K Cmd+I`: 内联 AI 辅助
- `Cmd+/`: 快速提问 (自定义)
- `Cmd+Shift+/`: 增强提示 (自定义)

---

## 📊 对比分析

### Auggie CLI vs Zed 集成

| 特性 | Auggie CLI | Zed 集成 |
|------|-----------|----------|
| 对话历史持久化 | ✅ 完整支持 | ❌ 不支持或有限 |
| 会话恢复 | ✅ `--continue` | ❌ 无法恢复 |
| 自定义规则 | ✅ 完整支持 | ✅ 支持 |
| 用户指南 | ✅ 支持 | ✅ 支持 |
| 代码上下文 | ⚠️ 需手动指定 | ✅ 自动获取 |
| 快捷键 | ❌ 命令行操作 | ✅ 可配置 |
| 工作流集成 | ⚠️ 需切换终端 | ✅ 无缝集成 |
| 自动化支持 | ✅ 脚本友好 | ❌ 有限 |

---

## 🎯 推荐方案

基于分析，我们推荐采用 **方案 C (混合方案)**:

### 短期解决方案 (立即可用)

1. **使用 Auggie CLI 处理重要对话**:
   ```bash
   # 在项目目录中
   cd /Users/Benson/Code/AlkaidSYS-tp
   auggie -c  # 恢复之前的对话
   ```

2. **配置项目规则**:
   - 创建 `.augment/rules/` 目录
   - 添加项目特定的规则文件
   - 将常用的指导原则写入规则

3. **使用 Zed 进行快速查询**:
   - 简单的代码解释
   - 快速的代码生成
   - 不需要保留历史的对话

### 长期优化方案

1. **建立知识库**:
   - 将重要的对话内容转化为 Rules
   - 定期更新 User Guidelines
   - 使用 Augment Memories 功能

2. **优化工作流**:
   - 为不同类型的任务选择合适的工具
   - 建立标准化的规则文件
   - 团队共享最佳实践

3. **关注更新**:
   - 定期检查 Augment 扩展更新
   - 关注 Zed 编辑器的 AI 功能改进
   - 参与社区讨论获取最新信息

---

## 📚 参考资源

### 官方文档
- [Augment CLI 参考](https://docs.augmentcode.com/cli/reference)
- [Augment Rules & Guidelines](https://docs.augmentcode.com/setup-augment/guidelines)
- [Zed 键位绑定文档](https://zed.dev/docs/key-bindings)
- [Zed AI 功能文档](https://zed.dev/docs/ai)

### 社区资源
- [Augment Code Reddit](https://www.reddit.com/r/AugmentCodeAI/)
- [Zed 编辑器 GitHub](https://github.com/zed-industries/zed)

### 配置文件位置
- Auggie CLI 配置: `~/.augment/settings.json`
- Zed 配置目录: `~/.config/zed/`
- 项目规则: `.augment/rules/`
- 项目指南 (旧): `.augment-guidelines`

---

## ✅ 行动清单

- [ ] 安装或更新 Auggie CLI
- [ ] 测试 `auggie -c` 恢复对话功能
- [ ] 创建项目 `.augment/rules/` 目录
- [ ] 编写项目特定的规则文件
- [ ] 配置 Zed 键位绑定 (如需要)
- [ ] 建立混合工作流程
- [ ] 将重要对话转化为规则
- [ ] 定期备份规则文件到版本控制

---

**报告生成时间**: 2025-11-24
**分析工具**: Augment Agent + Web Search + Documentation Review
**建议优先级**: 高 - 建议立即实施短期解决方案


